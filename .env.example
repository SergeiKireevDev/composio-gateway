HOST=127.0.0.1
PORT=8788
DATA_DIR=./data
# Set to the externally reachable origin when Oyster is on another machine.
PUBLIC_URL=https://gateway.example.com
SESSION_TTL_SECONDS=3600
# Optional. If omitted, a random token is generated in DATA_DIR/admin.token.
# ADMIN_TOKEN=use-a-long-random-secret
