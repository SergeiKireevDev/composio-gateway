{
  "name": "composio-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check admin-auth.mjs && node --check store.mjs && node --check composio.mjs && node --check public/app.js"
  },
  "dependencies": {
    "@composio/core": "0.18.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
